# manager-act-on-behalf

Let a manager with a full grant act on a client's portfolio directly, through a single audited write path: every manager mutation is recorded as a change_request (auto-approved + applied instantly when the grant is full; left pending for client approval when the grant is view-only).
