# Copilot instructions

Canonical repo rules, verified commands, directory map, and out-of-scope paths: **`AGENTS.md`**.

Path-scoped guidance: `.github/instructions/*.instructions.md`.

Do not trust `.cursorrules` for stack facts — it is stale (Convex). Trust `AGENTS.md`, source code, and `.github/workflows/ci.yml`.
